```diff
--- a/tests/drills/drill_operator_readiness.py
+++ b/tests/drills/drill_operator_readiness.py
@@ -1,5 +1,6 @@
 #!/usr/bin/env python3
 """
+Drill test for operator-readiness.
 
 Drill: operator-readiness — honest scorecard of the agent/governance stack.
 
@@ -208,4 +209,10 @@ if __name__ == "__main__":
     sys.exit(main())
 
 # Add a simple assertion to check if all steps passed
+assert step(1, "Archon CLI on $PATH", archon_bin is not None)
+assert step(2, "archon doctor: DB reachable + workspace writable + ≥1 AI provider OK", has_db and has_workspace and has_provider)
+assert step(3, "NEG: archon doctor reports gh CLI NOT configured (gap surfaced)", gh_not_configured)
+assert step(4, "NEG: Slack + Telegram tokens NOT set (notification gap surfaced)", slack_missing and telegram_missing)
+assert step(5, f"all {len(workflows)} BEV Archon workflows have ≥1 approval: node", workflows and not missing_approval)
+assert step(6, "BMad runnable via scripts/bmad.sh (closes Node-18 blocker via nvm shim)", bmad_ok)
+assert step(7, "voice pipeline tools present (Piper model cached + whisper importable)", piper_model.exists() and whisper_ok)
+assert step(8, "scripts/governance_diff_check.sh exists + executable", gov_check.exists() and os.access(gov_check, os.X_OK))
+assert step(9, "CODEOWNERS maps backend/frontend/.archon/.claude", not missing_areas)
+assert step(10, "NEG: ~/.archon/.env is chmod 600 (no world/group read on creds)", is_600)
```