# Models — Global Context

**Map position:** `global-ai-org → app-stack → backend → app → models`

## How this folder fits the global org standard

This folder is part of the canonical `global-ai-org/` structure (1,074 directories)
defined by global CLAUDE.md §63 and `~/.claude/policies/global-ai-org-structure.md`.

**Parent chain:**
- `app-stack/` — see its `README.md` for purpose
- `app-stack/backend/` — see its `README.md` for purpose
- `app-stack/backend/app/` — see its `README.md` for purpose

**Root spec:** `~/.claude/templates/global-ai-org/` (reference copy)
**Scaffold script:** `~/.claude/scripts/scaffold-global-ai-org.py`
**Local purpose:** see `README.md` in this same folder.

> Every folder in the org carries BOTH `README.md` (local scope)
> AND `GLOBAL_README.md` (this file — global-map position). Operator rule
> per §63 — neither file is optional.
