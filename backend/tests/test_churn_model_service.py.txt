<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/tests/test_churn_model_service.py
# auto-generated explainer · 2026-06-10T10:01:16Z

## Purpose
test_churn_model_service.py — smoke + quality checks for the churn model.

Requires the customer pilot tables to be ingested (skips if empty).

## Imports
- from `__future__` import annotations
- from `_` import pytest
- from `repositories.customer_repo` import CustomerRepo
- from `services.churn_model_service` import ChurnModelService

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `svc()`: (no docstring)
- `test_fit_and_metrics(svc)`: (no docstring)
- `test_rank_top_n(svc)`: (no docstring)
- `test_predict_single_customer(svc)`: (no docstring)
- `test_predict_unknown_customer(svc)`: (no docstring)
- `test_predict_cached_same_result(svc)`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- Benchmark: IBM Telco GBM reaches AUC ~0.80–0.86.
- Random baseline for precision@top10% ≈ 0.27 (churn rate). We expect > 2× that.
- Each has keys expected by the frontend.
- Top of list should all be high probability (> 0.6).
- Sorted descending.
