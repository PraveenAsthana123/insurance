<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/ml/reference/anomaly_lifecycle.py
# auto-generated explainer · 2026-06-05T10:00:47Z

## Purpose
INSUR reference: anomaly-detection lifecycle with multi-detector comparison.

Per operator 2026-05-22 + §64.23 + §64.32: every dept needs anomaly detection.

Detectors compared on the same labeled (or label-injected) dataset:

  1. Z-score (univariate)         baseline
  2. IsolationForest (multivariate)
  3. One-Class SVM (multivariate)
  4. AutoEncoder reconstruction error (deep)
  5. LocalOutlierFactor (density-based)

Per detector: precision + recall + F1 + ROC-AUC + PR-AUC at the labeled
anomaly threshold + latency.

If no labels are provided, the lifecycle injects synthetic outliers (5%
of rows shifted by 3-5σ) so all metrics have ground truth.

Artifacts: data/evaluation/<dept>/<pipeline>/<run_id>/manifest.json + plots/

## Imports
- from `__future__` import annotations
- from `_` import json
- from `_` import logging
- from `_` import time
- from `_` import uuid
- from `dataclasses` import asdict, dataclass, field
- from `pathlib` import Path
- from `typing` import Any
- from `_` import matplotlib
- from `_` import matplotlib.pyplot

## Inputs
- CLI args: --dataset, --dept, --pipeline, --label-col, --contamination, --sample, --artifacts-root

## Process
### Classes
- `AnomalyManifest`: (no docstring)
- `AnomalyLifecycle`: (no docstring)
### Functions
- `_main()`: (no docstring)

## Outputs
- Writes files (detected 1 patterns)
- Return values from functions above

## Notable inline comments
- ROC + PR AUC (need scores, not labels)
- ------------------------------------------------------------------
- Data loading
- ------------------------------------------------------------------
- Extract numeric features
