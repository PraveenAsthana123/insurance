<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# tests/drills/drill_council_and_hitl.py
# auto-generated explainer · 2026-06-02T10:00:35Z

## Purpose
drill_council_and_hitl — lock §77 rows 1415 + 1416 deliveries.

Per global §43: ≥3 negative assertions. Per §73.1 #3: this drill runs
every time the council or HITL code changes.

Positive steps lock the contract; negative steps prove the system
rejects what it must reject.

## Imports
- from `__future__` import annotations
- from `_` import json
- from `_` import sys
- from `_` import time
- from `pathlib` import Path
- from `backend.core.hitl_approval` import ApprovalStatus, RiskLevel, decide, get, list_pending

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `check(label, cond, negative)`: (no docstring)
- `main()`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- ---- HITL approval gate (row 1416) ----
- ---- HITL negative: deny path ----
- ---- HITL negative: double-decide is no-op ----
- ---- HITL: unknown id raises KeyError ----
- ---- Council module import (row 1415) ----
