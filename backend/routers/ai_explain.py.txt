<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/routers/ai_explain.py
# auto-generated explainer · 2026-06-12T10:01:54Z

## Purpose
(no module docstring — add one to improve this)

## Imports
- from `__future__` import annotations
- from `_` import logging
- from `functools` import lru_cache
- from `fastapi` import APIRouter, HTTPException, Response, status
- from `core.structured_logger` import emit_event
- from `schemas.ai_explain` import ExplainRequest, ExplainResponse, FeedbackRequest
- from `services.rag_service` import CONTEXT_DIR, CUSTOMER_CONTEXT_DIR, SUPPLY_CHAIN_CONTEXT_DIR, RAGService

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `_rag_for(corpus)`: One RAGService singleton per corpus. Index builds lazily on first request.
- `get_rag_service()`: (no docstring)
- `explain(req)`: (no docstring)
- `feedback(req)`: Record a thumbs-up/down rating against a previous explain call.

## Outputs
- Return values from functions above

## Notable inline comments
- default = sales
- Backwards-compatible: tests or callers that bypass the corpus selector get sales.
