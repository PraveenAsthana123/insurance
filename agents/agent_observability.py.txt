<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# agents/agent_observability.py
# auto-generated explainer · 2026-06-12T10:00:03Z

## Purpose
(no module docstring — add one to improve this)

## Imports
- from `__future__` import annotations
- from `_` import hashlib
- from `_` import json
- from `_` import os
- from `_` import time
- from `pathlib` import Path
- from `typing` import Any

## Inputs
- Env vars: AGENT_TRACE_PATH

## Process
### Functions
- `_resolve_trace_path()`: Resolve where to write the trace JSONL.
- `prompt_hash(prompt)`: (no docstring)
- `classify_failure(result)`: (no docstring)
- `retry_policy(category, retry_count)`: (no docstring)
- `build_trace(queue, task, output, model, agent_id)`: Build an enriched trace row.
- `golden_rule_completeness(trace, context)`: Check the 16-field golden-rule completeness on a trace row.
- `append_trace(row)`: (no docstring)
- `attach_trace(output, trace)`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- Local-dev fallback: project-relative path (works for CLI + tests)
- ── §57.6.1 / §83.6 16-field golden-rule extension ───────────────
- Populate from output, then task, then defaults. Per §57.7 honesty,
- do not invent values; mark missing as empty string.
- ── Base schema (existing) ─────────────────────────────────
