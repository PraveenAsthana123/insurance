<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# scripts/kt_bmad_space.py
# auto-generated explainer · 2026-06-10T10:00:08Z

## Purpose
Create a lightweight KT + BMAD workspace and optionally submit it to OpenClaw.

## Imports
- from `__future__` import annotations
- from `_` import argparse
- from `_` import datetime
- from `_` import json
- from `_` import re
- from `_` import sys
- from `_` import urllib.error
- from `_` import urllib.request
- from `pathlib` import Path
- from `typing` import Any

## Inputs
- CLI args: --title, --text, --file, --department, --mode, --profile, --base-dir, --submit, --api-url, --base-dir

## Process
### Functions
- `slugify(text)`: (no docstring)
- `now()`: (no docstring)
- `read_prompt(args)`: (no docstring)
- `build_workspace(args)`: (no docstring)
- `render_kt(payload)`: (no docstring)
- `render_bmad_handoff(payload)`: (no docstring)
- `submit_openclaw(api_url, payload)`: (no docstring)
- `cmd_create(args)`: (no docstring)
- `cmd_list(args)`: (no docstring)
- `parser()`: (no docstring)
- `main()`: (no docstring)

## Outputs
- Writes files (detected 1 patterns)
- Return values from functions above
