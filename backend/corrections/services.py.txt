<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/corrections/services.py
# auto-generated explainer · 2026-06-13T10:03:10Z

## Purpose
Decision corrections services · T7.10.

## Imports
- from `__future__` import annotations
- from `_` import json
- from `_` import logging
- from `_` import uuid
- from `collections` import Counter
- from `datetime` import datetime
- from `typing` import Any, Optional
- from `_` import psycopg2
- from `_` import psycopg2.extras
- from `pydantic` import BaseModel, Field

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Classes
- `CorrectionCreate`: (no docstring)
- `Correction`: (no docstring)
- `CorrectionStats`: (no docstring)
### Functions
- `_conn()`: (no docstring)
- `_row(r)`: (no docstring)
- `create_correction(data, tenant_id, correlation_id)`: (no docstring)
- `list_corrections(tenant_id, run_ref, severity, limit)`: (no docstring)
- `stats(tenant_id)`: (no docstring)
- `get_correction(correction_ref, tenant_id)`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- ─── Schemas ─────────────────────────────────────────────
- ─── CRUD ────────────────────────────────────────────────
