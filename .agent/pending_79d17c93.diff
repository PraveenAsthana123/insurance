```diff
--- a/tests/drills/drill_openclaw_paperclip_federation.py
+++ b/tests/drills/drill_openclaw_paperclip_federation.py
@@ -140,6 +140,7 @@ def main() -> int:
             f"status={r.status_code} clip_still_exists={still_exists}")
 
         # ---- Step 8: NEG context-pack — foreign clip in missing_ids ----
+        step(8, "NEGATIVE: context-pack — foreign clip in missing_ids", False)
         r = client.post(
             "/api/v1/paperclip/context-pack",
             headers={"X-Tenant-ID": "tenant-b"},
```