<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# scripts/download_insurance_datasets.py
# auto-generated explainer · 2026-06-11T10:00:51Z

## Purpose
Insurance dataset downloader — pulls Kaggle datasets per dept's
INSUR_DATA_MGMT.md catalog. Per operator 2026-06-01 + global §36.

Skips datasets known to be (a) >500MB or (b) competition-only.
Writes a manifest at data/insurance/_manifest.json showing what succeeded.

Usage: python3 scripts/download_insurance_datasets.py [--dry-run]

## Imports
- from `__future__` import annotations
- from `_` import json
- from `_` import subprocess
- from `_` import sys
- from `datetime` import datetime, timezone
- from `pathlib` import Path

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `download(slug, dataset, dry_run, refresh)`: (no docstring)
- `main()`: (no docstring)

## Outputs
- Writes files (detected 1 patterns)
- Return values from functions above

## Notable inline comments
- (slug, kaggle_dataset, description, skip_reason or None)
- underwriting
- customer-service
- fraud-siu
- Skip if already present (idempotent) unless --refresh
