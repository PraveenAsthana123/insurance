<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# infra/observability/otel_instrumentation.py
# auto-generated explainer · 2026-06-10T10:00:32Z

## Purpose
OTel instrumentation bootstrap — call once at backend boot.

Per ADR-009 + global §57.6 (canonical fields) + Codex 2026-06-01.

Usage in your FastAPI app:
    from core.otel_instrumentation import setup_otel
    setup_otel(app, service_name="insur-backend")

## Imports
- from `__future__` import annotations
- from `_` import logging
- from `_` import os

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `setup_otel(app, service_name)`: Initialize OpenTelemetry + auto-instrument FastAPI / httpx / psycopg.

## Outputs
- Return values from functions above

## Notable inline comments
- Auto-instrument common libraries
