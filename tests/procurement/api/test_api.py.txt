<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# tests/procurement/api/test_api.py
# auto-generated explainer · 2026-06-12T10:01:50Z

## Purpose
Tier 3 — API tests for procurement.

Recommended: pytest + httpx + schemathesis.
Per global §64.30 tier 3: every endpoint × every status code.

## Imports
- from `_` import pytest

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `test_procurement_list_returns_200()`: REPLACE — GET /api/v1/insur/depts (smoke that backend reachable).
- `test_procurement_invalid_input_returns_400()`: REPLACE — verify bad input is rejected with 4xx (no silent success).

## Outputs
- Return values from functions above
