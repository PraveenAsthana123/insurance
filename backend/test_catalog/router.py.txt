<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/test_catalog/router.py
# auto-generated explainer · 2026-06-13T10:03:09Z

## Purpose
/api/v1/test-catalog/* · Iter 47 · pipelines + test agents + responsibility table.

## Imports
- from `__future__` import annotations
- from `pathlib` import Path
- from `_` import psycopg2
- from `_` import psycopg2.extras
- from `fastapi` import APIRouter
- from `core.config` import get_settings
- from `_` import os
- from `_` import time

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `_conn()`: (no docstring)
- `health()`: (no docstring)
- `pipelines()`: Categorized pipeline catalog · counts per category + entries.
- `responsibility_table()`: Per-process · owner agent · supporting skills · data source · model.
- `test_agents()`: Live test agents from DB + their skill counts.
- `top_1pct_plan()`: (no docstring)
- `quality_dimensions()`: (no docstring)
- `benchmarks()`: Per-dimension benchmark targets · for the scorecard.
- `scoring()`: Scoring rubric per dimension.
- `_top1_ttl()`: (no docstring)
- `top_1pct_report(force)`: Compute a LIVE top-1% scorecard across all 11 dimensions.
- `stats()`: One-shot rollup · test agents + pipeline counts + invocation history.

## Outputs
- Return values from functions above

## Notable inline comments
- ──────────────────────────────────────────────────────────────────────
- Static catalogs
- (process, owner_agent, supporting_skills, data_source, model_used)
- ──────────────────────────────────────────────────────────────────────
- Endpoints
