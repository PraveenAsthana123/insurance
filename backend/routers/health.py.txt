<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/routers/health.py
# auto-generated explainer · 2026-06-11T10:01:43Z

## Purpose
(no module docstring — add one to improve this)

## Imports
- from `__future__` import annotations
- from `fastapi` import APIRouter

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `_health_payload()`: (no docstring)
- `health_check_v1()`: (no docstring)
- `health_check_unversioned()`: Alias for backwards compat — keeps Docker HEALTHCHECK working.

## Outputs
- Return values from functions above

## Notable inline comments
- Versioned router — preferred for new clients
- Unversioned alias — keeps Docker healthcheck + load balancers stable
- even when /api/v1/* is added or moved. Per global §6.5 (versioning rule:
- /api/health stays unversioned to remain stable across version bumps).
