<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/agentic_core/router.py
# auto-generated explainer · 2026-06-10T10:01:16Z

## Purpose
/api/v1/agentic/* · Iter 37 · agent + skill + tool registries + invocation.

## Imports
- from `__future__` import annotations
- from `_` import json
- from `_` import uuid
- from `datetime` import datetime, timezone
- from `typing` import Any
- from `_` import psycopg2
- from `_` import psycopg2.extras
- from `fastapi` import APIRouter, Depends, HTTPException
- from `pydantic` import BaseModel
- from `core.config` import get_settings

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Classes
- `AgentCreate`: (no docstring)
- `SkillCreate`: (no docstring)
- `ToolCreate`: (no docstring)
- `MappingCreate`: (no docstring)
- `InvokeRequest`: (no docstring)
### Functions
- `_conn()`: (no docstring)
- `health()`: (no docstring)
- `create_agent(body)`: (no docstring)
- `list_agents(status, tenant_id, limit)`: (no docstring)
- `get_agent(agent_id)`: (no docstring)
- `create_skill(body)`: (no docstring)
- `list_skills(status, risk_level, tenant_id, limit)`: (no docstring)
- `create_tool(body)`: (no docstring)
- `list_tools(tool_type, tenant_id, limit)`: (no docstring)
- `create_mapping(body)`: (no docstring)
- `invoke_agent(body)`: Per §57.7 scaffold runtime · validates agent + plans skills + writes audit row.
- `list_invocations(agent_id, status, tenant_id, limit)`: (no docstring)
- `get_invocation(invocation_id)`: (no docstring)
- `invocation_stats(tenant_id)`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- ──────────────────────────────────────────────────────────────────────
- Pydantic models
- ──────────────────────────────────────────────────────────────────────
- Health + stats
- ──────────────────────────────────────────────────────────────────────
