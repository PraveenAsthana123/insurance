<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/tests/test_forecast_service.py
# auto-generated explainer · 2026-06-02T10:00:38Z

## Purpose
test_forecast_service.py — unit tests for ForecastService.

Uses synthetic sales data injected via a mock SalesRepo; no Postgres needed.

## Imports
- from `__future__` import annotations
- from `datetime` import date, timedelta
- from `unittest.mock` import MagicMock
- from `_` import pytest
- from `backend.services.forecast_service` import ForecastService, _mape

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `_make_synthetic_history(days)`: Generate deterministic sales series with weekly + trend signal.
- `mock_repo()`: (no docstring)
- `test_mape_zero_when_perfect()`: (no docstring)
- `test_mape_skips_zero_actual()`: (no docstring)
- `test_forecast_returns_schema(mock_repo)`: (no docstring)
- `test_forecast_mape_reasonable_on_synthetic(mock_repo)`: (no docstring)
- `test_cache_hit_skips_refit(mock_repo)`: (no docstring)
- `test_missing_history_raises(mock_repo)`: (no docstring)
- `test_insufficient_history_raises(mock_repo)`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- (|100-110|/100 + |200-210|/200) / 2 = (0.1 + 0.05)/2 = 0.075
- Synthetic data is easy — MAPE should be well below 30%.
- Second call reads cached _FittedModel so fit_time_ms is identical to first.
