<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# scripts/spec_kit.py
# auto-generated explainer · 2026-06-12T10:00:31Z

## Purpose
Repo-local Spec Kit: create governed specs and route them into BMAD/OpenClaw.

## Imports
- from `__future__` import annotations
- from `_` import argparse
- from `_` import datetime
- from `_` import json
- from `_` import re
- from `_` import subprocess
- from `_` import sys
- from `pathlib` import Path

## Inputs
- CLI args: --title, --text, --file, --department, --priority, --mode, --profile, --users, --requirements, --acceptance

## Process
### Functions
- `slugify(value)`: (no docstring)
- `read_text(args)`: (no docstring)
- `parse_lines(value)`: (no docstring)
- `workspace_path(base_dir, title)`: (no docstring)
- `build_payload(args)`: (no docstring)
- `render_spec(payload)`: (no docstring)
- `render_bmad_prompt(payload, spec_rel)`: (no docstring)
- `create_spec(args)`: (no docstring)
- `route_to_bmad(workspace, payload, submit)`: (no docstring)
- `cmd_create(args)`: (no docstring)
- `cmd_list(args)`: (no docstring)
- `parser()`: (no docstring)
- `main()`: (no docstring)

## Outputs
- Writes files (detected 1 patterns)
- Return values from functions above
