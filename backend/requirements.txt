# Web framework
fastapi>=0.110.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
python-multipart>=0.0.9,<1.0.0

# Database
psycopg2-binary>=2.9.9,<3.0.0
# psycopg v3 used by the Sales deep-dive (SalesRepo + ingest_rossmann.py)
psycopg[binary]>=3.1.0,<4.0.0

# Data ingestion (Kaggle flagship datasets)
kaggle>=1.6.0,<2.0.0

# Task queue
celery[redis]>=5.3.0,<6.0.0

# ML / AI
scikit-learn>=1.4.0,<2.0.0
xgboost>=2.0.0,<3.0.0
prophet>=1.1.5,<2.0.0
pandas>=2.2.0,<3.0.0
numpy>=1.26.0,<2.0.0
mlflow>=2.12.0,<3.0.0

# Config & validation
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# HTTP client
httpx>=0.27.0,<1.0.0
requests>=2.31.0,<3.0.0

# RAG / retrieval (BM25 lexical search alongside vector retrieval)
rank-bm25>=0.2.2,<1.0.0

# Testing
pytest>=8.0.0,<9.0.0
pytest-asyncio>=0.23.0,<1.0.0

# Agent platform — Playwright (§64.40 layer 8). Browser binaries via:
#   python -m playwright install chromium
# Code path falls back to status='unavailable' if missing.
playwright>=1.55.0,<2.0.0

# AI / Agent ecosystem (per docs/AI_AGENT_TOOLS_EVALUATION.md, §56 gate-1).
# All stage-1 tools are installed but NOT yet wired into production code paths.
# Wiring is operator-gated per §56.2 (Stage-1 adapter contract).
# Run scripts/techstack_audit.py to verify install + capture versions.
# Drill: tests/drills/drill_ai_agent_tools_installed.py (9 steps, 3 negative).
agentops>=0.4.0,<1.0.0
pydantic-ai>=1.0.0,<2.0.0
dspy-ai>=3.0.0,<4.0.0
haystack-ai>=2.20.0,<3.0.0
# arize-phoenix: REMOVED — no runtime import · transitive aiosqlite>=0.22 conflict with crewai aiosqlite~=0.21
langsmith>=0.3.0,<1.0.0
# crewai: REMOVED — no runtime import · transitive aiosqlite~=0.21 conflict with arize-phoenix aiosqlite>=0.22
pyautogen>=0.10.0,<1.0.0
# OpenHands is stage-3 (autonomous coding agent, local dev only, NOT runtime)
# openhands-ai: REMOVED — stage-3 dev-only · no runtime import · transitive e2b<0.18 conflict

# AI / Agent ecosystem — batch 2 (2026-05-26): orchestrators + LLM gateways +
# observability. Same §56 gate-1-only adoption — wiring deferred to operator.
dagster>=1.10.0,<2.0.0
prefect>=3.0.0,<4.0.0
kestra>=1.0.0,<2.0.0
wmill>=1.700.0,<2.0.0
portkey-ai>=2.0.0,<3.0.0
litellm>=1.70.0,<2.0.0
openlit>=1.40.0,<2.0.0
langfuse>=4.0.0,<5.0.0
