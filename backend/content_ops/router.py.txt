<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/content_ops/router.py
# auto-generated explainer · 2026-06-10T10:01:15Z

## Purpose
/api/v1/content-ops/* — postings + contacts + schedules + monitoring.

## Imports
- from `__future__` import annotations
- from `_` import logging
- from `_` import uuid
- from `typing` import Optional
- from `fastapi` import APIRouter, HTTPException, Request
- from `_` import services
- from `schemas` import BulkUploadRequest, BulkUploadResponse, Contact, ContactCreate, Posting

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `_corr(r)`: (no docstring)
- `_tenant(r)`: (no docstring)
- `health()`: (no docstring)
- `postings_monitoring(request)`: (no docstring)
- `list_postings(request, channel)`: (no docstring)
- `create_posting(body, request)`: (no docstring)
- `get_posting(posting_id, request)`: (no docstring)
- `patch_posting(posting_id, body, request)`: (no docstring)
- `publish(posting_id, body, request)`: (no docstring)
- `list_contacts(request, segment, limit)`: (no docstring)
- `create_contact(body, request)`: (no docstring)
- `bulk_upload_contacts(body, request)`: (no docstring)
- `list_schedules(request)`: (no docstring)
- `create_schedule(body, request)`: (no docstring)
- `patch_schedule(schedule_id, body, request)`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- ── HEALTH (defined first to avoid path shadow) ────────────────────
- ── MONITORING ────────────────────────────────────────────────────
- ── POSTINGS CRUD + publish ───────────────────────────────────────
- ── CONTACTS CRUD + bulk upload ──────────────────────────────────
- ── SCHEDULES ────────────────────────────────────────────────────
