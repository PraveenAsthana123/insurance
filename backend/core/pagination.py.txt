<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/core/pagination.py
# auto-generated explainer · 2026-06-12T10:01:52Z

## Purpose
Pagination helper · Iter 26 · C3 closure.

Standardized cursor + offset/limit response shape per §6.1 spec.

## Imports
- from `__future__` import annotations
- from `typing` import Any, Generic, Sequence, TypeVar
- from `fastapi` import Query
- from `pydantic` import BaseModel

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Classes
- `PaginationParams`: Common pagination query params · use as `params: PaginationParams = Depends()`.
- `PaginatedResponse`: Standard envelope · per §6.1.
### Functions
- `get_pagination(offset, limit)`: (no docstring)
- `paginate(items, total, offset, limit)`: Build the standard pagination envelope.

## Outputs
- Return values from functions above
