<!-- agent-py-sidecar: auto-generated; do not delete this line to allow refresh -->
# backend/voice_ai/services.py
# auto-generated explainer · 2026-06-12T10:01:53Z

## Purpose
Services + LangGraph DAG for the voice AI end-to-end flow.

Per §90 + §91 + §64.40 (10-layer agentic) + §64.43 #5 Blackboard pattern.

Layers (LangGraph-style state transitions):
  1. welcome      → Aria greets per template + segment
  2. identify     → resolve customer from ref / phone / email
  3. presales     → discovery questions per category
  4. requirement  → capture structured needs
  5. recommend    → match product from catalog + RAG
  6. order        → create order row + audit
  7. notify       → email/sms/voice confirmation
  8. complete     → close session

Each turn is deterministic · LLM is pluggable but optional (rule-based default
for demo · per §57.7 honest fallback).

## Imports
- from `__future__` import annotations
- from `_` import json
- from `_` import logging
- from `_` import time
- from `_` import uuid
- from `typing` import Any, Optional
- from `_` import psycopg2
- from `_` import psycopg2.extras
- from `core.config` import get_settings
- from `schemas` import Customer, MonitoringSnapshot, Order, Product, Session

## Inputs
(no CLI / env inputs — library module or implicit)

## Process
### Functions
- `_conn()`: (no docstring)
- `_row_to_dict(row)`: (no docstring)
- `list_products(tenant_id, category, enabled_only)`: (no docstring)
- `create_product(data, tenant_id)`: (no docstring)
- `delete_product(product_id, tenant_id)`: (no docstring)
- `find_customer(customer_ref, phone, email, tenant_id)`: (no docstring)
- `list_customers(tenant_id, limit)`: (no docstring)
- `list_welcome_templates(tenant_id)`: (no docstring)
- `pick_welcome(segment, tenant_id)`: (no docstring)
- `update_welcome(template_id, patch, tenant_id)`: (no docstring)
- `start_session(customer_ref, phone, email, tenant_id, correlation_id)`: (no docstring)
- `_save_session(sid, transcript, stage, requirements, recommended_id)`: (no docstring)
- `_next_stage(current)`: (no docstring)
- `_detect_category(text)`: (no docstring)
- `_match_product(requirements, segment, tenant_id)`: (no docstring)

## Outputs
- Return values from functions above

## Notable inline comments
- ─────────────────────────────────────────────────────────────────────
- Connection helper
- ─────────────────────────────────────────────────────────────────────
- ─────────────────────────────────────────────────────────────────────
- Product catalog
